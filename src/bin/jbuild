(jbuild_version 1)

(executable
  ((name        main)
   (public_name vpnkit)
   (package     vpnkit)
   (libraries   (cmdliner ofs logs.fmt hostnet hvsock hvsock.lwt-unix
                 datakit-server-9p win-eventlog asl fd-send-recv))
   (preprocess   no_preprocessing)))
